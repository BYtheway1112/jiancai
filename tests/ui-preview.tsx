import React,{useEffect,useRef,useState} from 'react';
import {createRoot} from 'react-dom/client';
import {Popup} from '../src/entrypoints/popup/Popup';
import {css} from '../src/collector/style';
import {toolbarServiceImages} from '../src/collector/ServiceLogo';
import {appearance,preferencePatch} from '../src/collector/preferences';
const targets:any[]=[{id:'video',name:'小红书视频',platform:'xhs',contentType:'video',base:'ExampleBase',table:'video',mapping:{}},{id:'image',name:'小红书图文',platform:'xhs',contentType:'image',base:'ExampleBase',table:'image',mapping:{}},{id:'dy',name:'抖音采集',platform:'dy',base:'ExampleBase',table:'dy',mapping:{}}];
let prefs:any={appearance:appearance(),targets:{'xhs:video':'video','xhs:image':'image',dy:'dy'}};
let notifyPreferences=()=>{};
const services:any={call:async(action:string,data:any)=>{if(action==='getConfig')return {appId:'cli_example_preview',hasSecret:true,targets};if(action==='preferences')return prefs;if(action==='listPosts')return Array.from({length:27});if(action==='savePreferences'){const patch=preferencePatch(data,targets);prefs={...prefs,appearance:appearance({...prefs.appearance,...patch.appearance}),targets:{...prefs.targets,...patch.targets}};notifyPreferences();return prefs;}if(action==='eagleInfo')return {libraryId:'demo',library:'灵感收藏库',folders:[{id:'inspiration',name:'设计灵感'},{id:'video',name:'影像参考'}]};if(action==='saveEagleFolder'){prefs.eagle=data;return prefs;}if(action==='saveConfig')return true;throw new Error('此预览不连接实际应用');},openSettings:()=>alert('这是独立视觉预览；正式插件会打开真实采集列表与设置。'),close:()=>alert('正式插件中此按钮会关闭弹窗。')};
function App(){const [revision,setRevision]=useState(0);useEffect(()=>{notifyPreferences=()=>setRevision(v=>v+1);return ()=>{notifyPreferences=()=>{}};},[]);const toolbar=useRef<HTMLDivElement>(null);useEffect(()=>{const s=toolbar.current!.shadowRoot||toolbar.current!.attachShadow({mode:'open'});const show=prefs.appearance;s.innerHTML=`<style>${css}</style><div class="bar" aria-label="简采工具栏">${show.download?'<button>↓ 下载</button>':''}${show.feishu?`<button><img class="service-logo" alt="" src="${toolbarServiceImages.feishu}"/>飞书</button>`:''}${show.eagle?`<button><img class="service-logo" alt="" src="${toolbarServiceImages.eagle}"/>Eagle</button>`:''}${show.more?'<details><summary>更多 ···</summary><div class="menu"><button>复制信息</button><button>导出评论</button><button>采集列表 / 设置</button></div></details>':''}</div>`;},[revision]);return <div className="preview-layout"><aside><img className="large-logo" src="/icon/logo.svg"/><p className="eyebrow">简采 · 界面改版</p><h2>轻一点。<br/>收得更完整。</h2><p className="intro">圆角卡片组织设置，半透明工具栏留在作品旁。<br/>采集、下载、飞书与 Eagle，各有清晰的位置。</p><div className="scene"><div ref={toolbar}/><div className="scene-caption">页面上的功能入口</div><div className="abstract-card one"/><div className="abstract-card two"/><div className="scene-title">灵感值得留下，<br/>操作可以很轻。</div></div><small className="preview-note">独立视觉预览 · 示例数据 · 不连接真实账户</small></aside><div className="popup-frame"><Popup services={services}/></div></div>};createRoot(document.getElementById('root')!).render(<App/>);
